requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0
# El diagnóstico de la corrida (informe_corrida.py) deja un Excel.
openpyxl>=3.1
# Solo para scripts/construir_indice_osm.py (el índice local de direcciones);
# la corrida diaria no lo necesita.
osmium>=3.6
